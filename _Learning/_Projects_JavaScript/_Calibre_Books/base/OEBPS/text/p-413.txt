           394                     Algorithms: A Top-Down Approach
           whereM isad       ×d matrixfor1 ≤i ≤ n.Because matrix multiplication is
                   i      i−1   i
           associative, we have some choice over the order in which the multiplications
           are performed. For example, to compute M M M , we may either
                                                      1  2  3
            • ﬁrst compute M M , then multiply on the right by M ;or
                              1  2                                 3
            • ﬁrst compute M M , then multiply on the left by M .
                              2  3                                1
           In other words, we may compute either (M M )M or M (M M ).
                                                     1   2   3     1   2  3
               Nowsupposed =2,d =3,d =4,andd =1.Thenthethreematrices
                             0       1      2           3
           are dimensioned as follows:
            • M :2×3;
                1
            • M :3×4; and
                2
            • M :4×1.
                3
           If we compute (M M )M , we ﬁrst multiply a 2×3matrixbya3×4matrix,
                            1  2   3
           then we multiply the resulting 2 × 4matrixbya4× 1 matrix. The total
           number of scalar multiplications is
                                     2·3·4+2·4·1=32.
           On the other hand, if we compute M (M M ), we ﬁrst multiply a 3 × 4
                                                 1   2  3
           matrix by a 4×1 matrix, then we multiply the resulting 3× 1matrixbya
           2×3matrix. The total number of scalar multiplications is
                                     3·4·1+2·3·1=18.
               Thus, the way in which the matrices are parenthesized can aﬀect
           the number of scalar multiplications performed in computing the matrix
           product. This fact motivates an optimization problem: Given a sequence of
           positive integer dimensions d ,...,d , determine the minimum number of
                                        0      n
           scalar multiplications needed to compute the product M ...M , assuming
                                                                   1      n
           M is a d     ×d matrix for 1 ≤ i ≤ n, and that the number of scalar
             i       i−1    i
           multiplications required to multiply two matrices is as described above.
               Various greedy strategies might be applied to this problem, but none
           can guarantee an optimal solution. Let us therefore look for a direct
           top-down solution to the problem of ﬁnding the minimum number of
           scalar multiplications for a product M ...M . Let us focus on ﬁnding the
                                                 i      j
           last matrix multiplication. This multiplication will involve the products
           M...M and M          ...M for some k,1≤ k<n. The sizes of these two
             i     k        k+1      j
           matrices are d   ×d and d ×d . Therefore, once these two matrices are
                         i−1    k      k    j
           computed, an additional d    d d scalar multiplications must be performed.
                                     i−1 k j
           The principle of optimality clearly holds for this problem, as a better way
